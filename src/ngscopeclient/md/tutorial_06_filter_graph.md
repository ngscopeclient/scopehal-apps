# Filter Graph

The filter graph is the main interface for performing protocol decodes, math, and analysis on waveform data.

The filter graph is docked to the main window next to your starting waveform group by default. Switch to this tab to begin analyzing your signals.
